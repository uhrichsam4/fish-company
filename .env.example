# Copy to .env and fill in. .env is gitignored and must never be committed.
# On Render these are set in the dashboard under Environment, not in the repo.
ELEVENLABS_API_KEY=
ELEVENLABS_API_KEY_BACKUP=
