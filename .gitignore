node_modules/
dist/
.DS_Store
*.log
.vite/
tmp/

# Automated visual-inspection captures: large, regenerable, and they filled the disk.
tools/shots/

# Secrets — never commit. Keys live in the environment / Render dashboard.
.env
.env.*
!.env.example

# Generated audio cache (regenerable, and large)
public/assets/audio/generated/

# Raw model sources. Multi-hundred-MB .blend/.obj/4K-texture drops get
# converted to compressed GLB in public/assets/models/ and only that ships --
# a public repo Render deploys from should not carry 350 MB of source art.
assets/incoming/
*.blend1
